# GitHub Actions default permissions
# https://docs.github.com/en/actions/security-guides/automatic-token-authentication

# This file is disabled - permissions should be set in individual workflows
# To use as a reusable workflow, uncomment the lines below and add proper jobs section

# defaults:
#   run:
#     # Set default shell
#     shell: bash

# permissions:
#   actions: read
#   checks: write
#   contents: write
#   deployments: write
#   discussions: write
#   id-token: write
#   issues: write
#   packages: write
#   pages: write
#   pull-requests: write
#   repository-projects: write
#   security-events: write
#   statuses: write